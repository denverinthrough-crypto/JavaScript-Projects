
let Sent1 = "This is the beginning of the string";  // First part of the message
let Sent2 = " and this is the end of the string";   // Second part of the message


let fullMessage = Sent1 + Sent2; // Combine Sent1 and Sent2

// Display the concatenated string in an alert
window.alert(fullMessage);  // Popup alert showing the full message

//  Display the concatenated string on the web page
document.write(fullMessage); // Writes the message directly into the HTML page

// Comments are included to explain each step of the code
